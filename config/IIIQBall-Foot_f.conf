# ===== 基本設定 =====
CONFIG_ZMK_BLE=y
CONFIG_ZMK_SPLIT=y

# 左右（子機）の情報を取得するための設定
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# ポインティング・Studio設定
CONFIG_ZMK_POINTING=y
CONFIG_INPUT=y
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# ===== Prospector (Status Advertisement) 設定 =====
CONFIG_ZMK_STATUS_ADVERTISEMENT=y

# 名前は最大8文字。13文字(IIIQBall-foot)だとパケットが溢れる可能性があるため短縮
CONFIG_ZMK_STATUS_ADV_KEYBOARD_NAME="IIIQBall"

# 表示位置の設定（ドングルを左端にする場合は "LEFT"）
CONFIG_ZMK_STATUS_ADV_CENTRAL_SIDE="LEFT"

# 電力と更新頻度
CONFIG_ZMK_STATUS_ADV_ACTIVITY_BASED=y
CONFIG_ZMK_STATUS_ADV_ACTIVE_INTERVAL_MS=100
CONFIG_ZMK_STATUS_ADV_IDLE_INTERVAL_MS=30000

# WPM計算ウィンドウ (秒)
CONFIG_ZMK_STATUS_ADV_WPM_WINDOW_SECONDS=30

# ===== 3分割用のスロット割り当て =====
# エラー回避のためコメントを値の横から移動しました。
# 左右が逆になった場合は、この 0 と 1 を入れ替えてください。

# 左手側(l)を想定
CONFIG_ZMK_STATUS_ADV_HALF_PERIPHERAL=0

# 右手側(r)を想定
CONFIG_ZMK_STATUS_ADV_AUX1_PERIPHERAL=1